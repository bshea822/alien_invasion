if event.key == pygame.K_RIGHT:
    # Halt the ship from moving right.
    self.ship.moving_right = False
elif event.key == pygame.K_LEFT:
    # Halt the ship from moving left.
    self.ship.moving_left = False


if event.key == pygame.K_RIGHT:
    # Halt ship from moving right.
    self.ship.moving_right == False
elif event.key == pygame.K_LEFT:
    # Halt ship from moving left.
    self.ship.moving_left = False
